version: "3.9"

services:
  application:
    build: .
    container_name: email-service-portfolio
    ports:
      - "8080:8080"
    environment:
      SUPPORT-EMAIL: "test-email@gmail.com"
      APP-PASSWORD: "test-pass"
      MY_MAIL_ADDRESS: "alinurmammadzada1@gmail.com"
